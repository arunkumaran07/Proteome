% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProteomicsML.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot of differential expression results}
\usage{
plot_volcano(de, p_cut = 0.05, fc_cut = 1)
}
\arguments{
\item{de}{A differential expression tibble as returned by
\code{\link{diff_expr_ttest}} or \code{\link{diff_expr_vs}}, with columns
\code{Gene}, \code{log2FC}, \code{padj}.}

\item{p_cut}{Adjusted p-value significance cutoff, used for the plotted
threshold line(s). Default \code{0.05}.}

\item{fc_cut}{Absolute log2 fold-change cutoff, used for the plotted
threshold line(s). Default \code{1}.}
}
\value{
A ggplot object (or an \pkg{EnhancedVolcano} plot object).
}
\description{
Plots log2 fold-change against adjusted p-value. Uses
\pkg{EnhancedVolcano} if installed, otherwise falls back to a plain
\pkg{ggplot2} volcano plot.
}
\examples{
\dontrun{
csv_path <- system.file("extdata", "example_proteome.csv", package = "ProteomicsML")
io <- read_wide_proteomics(csv_path)
de <- diff_expr_ttest(io$expr, io$groups, ref = levels(io$groups)[1])
plot_volcano(de)
}
}
