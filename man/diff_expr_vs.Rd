% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProteomicsML.R
\name{diff_expr_vs}
\alias{diff_expr_vs}
\title{Differential expression: reference vs. one specific target group}
\usage{
diff_expr_vs(expr, groups, ref, target)
}
\arguments{
\item{expr}{Numeric matrix of expression values, proteins x samples.}

\item{groups}{Factor of group labels, length \code{ncol(expr)}.}

\item{ref}{Reference group label.}

\item{target}{Target group label to compare against \code{ref}.}
}
\value{
A tibble with columns \code{Gene}, \code{log2FC}, \code{pvalue},
\code{padj}, as returned by \code{\link{diff_expr_ttest}}.
}
\description{
Subsets \code{expr}/\code{groups} to just the \code{ref} and \code{target}
groups, then runs \code{\link{diff_expr_ttest}}.
}
\examples{
\dontrun{
csv_path <- system.file("extdata", "example_proteome.csv", package = "ProteomicsML")
io <- read_wide_proteomics(csv_path)
levs <- levels(io$groups)
de <- diff_expr_vs(io$expr, io$groups, ref = levs[1], target = levs[2])
}
}
